import { Group } from './group.entity';
import { Subject } from './subject.entity';
import { Person } from './person.entity';
import { Mark } from './mark.entity';

export const entities = [Group, Subject, Person, Mark];
